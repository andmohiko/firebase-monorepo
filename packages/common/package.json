{
  "name": "@firebase-monorepo/common",
  "version": "0.0.0",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "lint:tsc": "tsc --noEmit",
    "lint:eslint": "eslint src --ext .js,.ts",
    "lint:prettier": "prettier --check src",
    "lint:cspell": "cspell -c ../../cspell.json src/**/*",
    "test": "vitest"
  },
  "main": "./src/index.ts",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.1",
    "@types/node": "20",
    "typescript": "5.3.3",
    "vitest": "^1.2.1"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=6.0.0"
  },
  "dependencies": {
    "firebase": "^10.12.2"
  }
}
